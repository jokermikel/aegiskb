# AegisKB 测试操作

本文用于本地验收聊天页面、会话管理、权限控制、高风险审批和 Qwen API Key 配置。

## 1. 启动与基础检查

在项目根目录执行：

```powershell
$env:PYTHONPATH = (Get-Location).Path
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

浏览器打开 <http://127.0.0.1:8000/>，另开一个终端执行：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/health
```

预期：页面可以打开，服务状态返回 `{"status":"ok"}`。

## 2. 登录测试

使用以下任一演示账号登录，密码均为 `demo-password`：

| 账号 | 预期角色 | 预期密级 |
| --- | --- | --- |
| `employee@example.com` | employee | INTERNAL |
| `finance@example.com` | finance | CONFIDENTIAL |
| `admin@example.com` | admin | CONFIDENTIAL |

预期：登录后进入聊天工作台，右上角显示当前密级；输入错误密码时显示“邮箱或密码错误”。

## 3. 会话管理测试

1. 点击“新建对话”，输入 `差旅报销测试` 并保存。
2. 确认左侧“最近对话”出现该名称，右侧有修改和删除按钮。
3. 在输入框发送：`国内出差住宿标准是多少？`
4. 点击“新建对话”，创建 `远程办公测试`。
5. 点击左侧 `差旅报销测试`，确认之前的问题和回答恢复。
6. 点击第一个会话的修改按钮，改名为 `差旅报销 2026`，确认列表和顶部标题同步更新。
7. 点击删除按钮并确认，确认该会话从列表消失。
8. 删除最后一个会话，确认列表可以为空，不会自动重新生成“企业制度问答”。
9. 刷新页面，确认已删除的会话不会恢复；点击“新建对话”仍可创建新会话。

预期：会话名称、消息和当前选中会话在刷新后保留；不同登录邮箱看不到其他用户的会话。

## 4. 权限过滤测试

以 `employee@example.com` 登录，提问：

```text
国内出差住宿标准是多少？
```

预期：回答不会引用 `2026 差旅报销制度`，因为该制度为 CONFIDENTIAL 且属于 finance 部门。

退出后以 `finance@example.com` 登录，重复提问。

预期：回答可以引用 `2026 差旅报销制度`，显示一线城市 800 元、其他城市 500 元等内容。

## 5. 高风险审批测试

以 employee 身份发送：

```text
请导出全部客户数据
```

预期：页面显示审批单编号，不执行导出。点击左侧 **审批列表**，能够看到本人刚提交的请求、`待处理` 状态和待处理数量，不显示“批准”或“拒绝”按钮。

退出后以 `finance@example.com` 登录，打开 **审批列表**。

预期：不能看到 employee 刚才提交的审批单，因为普通用户只能查看本人记录。

退出后以 `admin@example.com` 登录，打开 **审批列表**。

预期：管理员能看到 employee 提交的审批单。先不填写意见直接点击“批准”，页面应提示至少填写 2 个字符；填写 `测试批准` 后点击“批准”，状态变为 `已批准`，操作按钮消失。再创建一条高风险请求，验证“拒绝”后状态变为 `已拒绝`。

接口层也需要验证。以 `admin@example.com` 登录 Swagger（<http://127.0.0.1:8000/docs>），调用：

```text
POST /api/v1/approvals/{approval_id}/decision
```

请求体：

```json
{"decision":"APPROVED","comment":"测试批准"}
```

预期：admin 首次处理返回 `200`；employee 调用决策接口返回 `403`；对同一已处理审批再次调用返回 `409`。即使手工修改前端、强行显示审批按钮，后端仍拒绝非管理员操作。

## 6. API Key 测试

1. 登录聊天页面，点击右上角 **API Key**。
2. 输入一个明显无效的测试值，例如 `sk-invalid-demo`，点击 **测试联通**。
3. 预期显示连接失败和上游错误（例如 `InvalidApiKey`），不能显示“连接成功”。
4. 输入阿里云百炼控制台创建的真实 Key，点击 **测试联通**。
5. 预期显示“连接成功，API Key 可用”，并显示当前模型；点击保存后发送一条普通制度问题。

注意：真实 API Key 不要写入本文档、代码、截图或 Git。前端只在当前浏览器 `sessionStorage` 保存，退出登录会清除；后端不会把它写入数据库或审计日志。

## 7. API 直接测试

先登录获取 token：

```powershell
$login = Invoke-RestMethod http://127.0.0.1:8000/api/v1/auth/login -Method Post -ContentType 'application/json' -Body '{"email":"finance@example.com","password":"demo-password"}'
$headers = @{ Authorization = "Bearer $($login.access_token)" }
```

测试 API Key 接口（不要把真实 Key 保存到脚本）：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/v1/settings/qwen/test -Method Post -Headers $headers -ContentType 'application/json' -Body '{"api_key":"sk-invalid-demo"}'
```

预期：无效 Key 返回 `connected: false`。当前 FastAPI `HTTPBearer` 对未携带认证头的调用返回 `403`，Key 少于 8 个字符应返回 `422`。

## 8. 自动化回归

```powershell
$env:PYTHONPATH = (Get-Location).Path
pytest -q
```

预期：显示 `7 passed`。当前测试覆盖权限过滤、审批列表的跨用户隔离、管理员批准/拒绝、非管理员 `403`、重复审批 `409`、Qwen 工具参数和 API Key 测试接口；自动化测试会显式清空 `DASHSCOPE_API_KEY`，不会访问真实模型。
